apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nginxconfigmap-volume 
  labels:
    app: nginx
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: slow
